%p#notice= notice
%h1 Listing Pages
%table
  %thead
    %tr
      %th Blob
      %th Name
      %th Content
      %th{:colspan => "3"}
  %tbody
    - @pages.each do |page|
      %tr
        %td= page.blob
        %td= page.name
        %td= page.content
        %td= link_to 'Show', [@wiki, page]
        %td= link_to 'Edit', edit_wiki_page_path(@wiki, page)
        %td= link_to 'Destroy', [@wiki, page], method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Page', new_wiki_page_path
