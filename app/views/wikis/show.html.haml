= render 'shared/wiki_side'

.markdown-content-body
  = link_to 'New Page', new_wiki_page_path(@wiki), class: 'pure-button pure-button-primary'

  %ul
    - @wiki.pages.each do |page|
      %li
        = link_to page.name, wiki_page_path(@wiki, page)
