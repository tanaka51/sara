.header
  %h1
    wikis
  %h2
    here is many wikis
= link_to 'New Wiki', new_wiki_path, class: 'pure-button pure-button-primary'

- @wikis.each do |wiki|
  .wiki-list.pure-g
    .pure-u-1-2
      %h2
        = link_to wiki.name, wiki_path(wiki)
    .pure-u-1-2
      = link_to 'edit', edit_wiki_path(wiki), class: 'pure-button'
